# =============================================================================
# .env.local — Variables d'environnement pour le développement local
# =============================================================================

# --- Google OAuth (local) ---
# Configurer dans Google Cloud Console > Credentials > OAuth 2.0 Client IDs :
#   - Authorized redirect URIs : http://localhost:54321/auth/v1/callback
#   - Authorized JavaScript origins : http://localhost:54321
GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_WEB_CLIENT_SECRET=GOCSPX-your-secret

# --- Supabase (client-side) ---
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
# EXPO_PUBLIC_DISABLE_PREFETCH=true              # (optionnel) Désactive le prefetch A/B (réduit les tokens de moitié)

# --- LLM (server-side, injecté dans les Edge Functions) ---
LLM_API_URL=http://localhost:11434/v1            # Ollama (dev)
LLM_MODEL=llama3:8b                              # Ollama (dev)
LLM_API_KEY=                                     # Vide pour Ollama
# LLM_PROVIDER=                                  # (optionnel) Force le provider : openai | gemini | openrouter
# LLM_CACHE_TTL=3600                             # (optionnel) TTL cache contexte Gemini en secondes (0 = désactivé)

# --- Gemini (alternative au LLM local) ---
# LLM_API_URL=https://generativelanguage.googleapis.com/v1beta
# LLM_MODEL=gemini-2.5-flash
# LLM_API_KEY=AIza...

# OpenRouter (décommenter pour utiliser OpenRouter à la place)
# LLM_API_URL=https://openrouter.ai/api/v1
# LLM_MODEL=anthropic/claude-sonnet-4-5-20250929
# LLM_API_KEY=sk-or-...
